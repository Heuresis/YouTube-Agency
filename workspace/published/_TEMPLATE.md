# PUBLISHED — "<title>"

```
Published:    <YYYY-MM-DD>  ·  Length <mm:ss>  ·  @<channel-handle>
Mode/Format:  <authority|reach|hybrid> · <format name per workspace/CONTENT-ENGINE.md> · <TOF|MOF|BOF>
Thumbnail:    "<thumbnail text>" (<concept/style>)
Status:       LIVE  ·  <A/B test running? launch window? CTA destination?>
```

> Duplicate this file → `workspace/published/<YYYY-MM-DD>--<slug>.md` (publish date in the name). Fill within ~48h of publish; backfill the metrics at 14 / 30 / 54 days. Then add a row to [`_ANALYTICS.md`](_ANALYTICS.md) — this record is the per-video detail behind that scoreboard row.

---

## Performance (since published, as of <date> · <N days>)

| Metric | Value | Read |
|---|---|---|
| Views | <n> (<unique>) | |
| Impressions / CTR | <n> / <%> | target ≥6%, floor 4% (company.yaml) |
| Avg view duration | <mm:ss> | |
| Avg % viewed | <%> | the retention tell (INV-9 floor) |
| Watch time | <hrs> | |
| Subscribers | +<n> | |
| Likes | <%> | vs <channel avg %> |

**Traffic:** <browse / suggested / search / external breakdown>. **Audience:** <new / casual / regular %>. **Device:** <breakdown>.

---

## What it put on record (citable claims)

Every number/claim now public on camera or in the packaging, mapped to the bank:

- <public claim> → <`workspace/PROOF-BANK.md` row it traces to / promotes>. <If it does NOT match the bank, flag it in `_ANALYTICS.md` → "data flags to reconcile" — surface to the creator, don't self-resolve (INV-6).>

## Why it worked / what to copy

- <packaging read: which title pattern + thumbnail concept, and the CTR verdict>
- <hook read: did the 0:30 hold beat the channel band?>
- <format read: what this format did that the last one didn't>

## What it taught us (→ the slate)

- <retention diagnosis: where the curve dropped, what the fix-path is — run `/audit-retention` if the drop is structural>
- <A/B result, if one ran — log it in `_ANALYTICS.md` too>
- <next-video implication: which `workspace/pipeline/SLATE.md` row this promotes, demotes, or creates>

---

## Full transcript (raw, for `/repurposing-cascade`)

<paste the transcript — keep the specifics that must survive repurposing: every number, tool name, and mechanism line verbatim>

---

*Logged <date> by <who> from <source — e.g. YouTube Studio export>.*
