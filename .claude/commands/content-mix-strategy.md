---
description: Calibrate the channel's authority/reach/hybrid mode mix and brand-pillar-split per channel-stage and audience.
---

Read `skills/content-mix-strategy/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
