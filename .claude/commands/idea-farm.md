---
description: Maintain a rolling backlog of 20+ video ideas sourced from comments, search, suggested, trends, sales calls.
---

Read `skills/idea-farm/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
