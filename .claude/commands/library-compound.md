---
description: Extract patterns from published videos — hook tiers, retention archetypes, conversion drivers — and update compartments.
---

Read `skills/library-compound/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
