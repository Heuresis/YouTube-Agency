---
description: Architect a multi-video series (5-30 videos) that installs the offer's belief sequence and routes to the back-end.
---

Read `skills/series-architect/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
