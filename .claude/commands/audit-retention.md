---
description: Diagnose a published video's retention curve, identify drop zones, and produce a fix-path.
---

Read `skills/audit-retention/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
