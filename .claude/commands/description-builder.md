---
description: Build the video description with timestamped chapters, CTA stack, and link architecture.
---

Read `skills/description-builder/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
