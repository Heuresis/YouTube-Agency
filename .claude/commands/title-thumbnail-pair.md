---
description: Produce title + thumbnail as one inseparable unit, predicting CTR and aligning the promise with first-30s script.
---

Read `skills/title-thumbnail-pair/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
