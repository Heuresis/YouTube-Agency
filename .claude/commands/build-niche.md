---
description: Define the channel's primary niche, sub-niche, adjacent reach niches, and out-of-scope guard.
---

Read `skills/build-niche/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
