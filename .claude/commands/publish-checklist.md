---
description: Pre-publish gate — verify all 17 invariants + production assets + distribution surface before upload.
---

Read `skills/publish-checklist/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
