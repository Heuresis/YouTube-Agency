---
description: Design the loop architecture for a script — open by 0:08, close before mid-point, callback in close, ≥3 loops per long-form.
---

Read `skills/architect-loops/SKILL.md` and execute its Process section. Apply the persona overlay of the agent declared in `agent_executor`. Verify per the skill's `verification_layers`. Return output in the declared Output Format.
